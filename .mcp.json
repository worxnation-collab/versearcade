{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y", "@playwright/mcp@latest",
        "--browser", "chromium",
        "--headless",
        "--isolated",
        "--executable-path", "/opt/pw-browsers/chromium-1194/chrome-linux/chrome"
      ]
    }
  }
}
