<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- OAuth deep link: system browser returns Google/Apple sign-in to the
                 app via com.versearcade.app://auth/callback. Capacitor's App plugin
                 fires 'appUrlOpen', handled in src/lib/native.ts (same as iOS). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.versearcade.app" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Church tab: a one-shot coarse fix so we can look up churches near the
         player. Never stored, never tracked in the background — fine location
         is declared only because some devices report nothing without it.
         Foreground only: ACCESS_BACKGROUND_LOCATION is deliberately absent, and
         it is the one that would drag Play's sensitive-permission declaration
         form in. Both of these still have to be disclosed in Data safety. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- @capacitor/local-notifications merges SCHEDULE_EXACT_ALARM into the
         manifest for every app that uses it. We don't want it: Play asks you to
         justify exact alarms, Android 14 makes the grant revocable, and a daily
         verse nudge does not need to-the-minute delivery. The plugin already
         degrades correctly — LocalNotificationManager checks
         canScheduleExactAlarms() and falls back to setAndAllowWhileIdle(), so
         reminders still fire, just not to the second. Removing it here keeps the
         permission off the Play listing entirely. -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:node="remove" />
</manifest>
