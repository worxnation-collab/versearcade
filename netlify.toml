[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (/play, /groups, ...) resolve to index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
